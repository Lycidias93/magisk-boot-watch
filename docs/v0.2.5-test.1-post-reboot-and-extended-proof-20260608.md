# Boot Watch Collector v0.2.5-test.1 installed proof — 2026-06-08

This document records the installed Pixel validation for `v0.2.5-test.1` after the prerelease was flashed through Magisk and rebooted.

## Scope

- Device role: Pixel / Magisk test host.
- Release under test: `v0.2.5-test.1`.
- Stable update channel: unchanged; `update.json` should remain on `v0.2.4` until a stable release decision.
- DNS/HA/VIP/route: not changed.

## Evidence sources

| Evidence | Path |
| --- | --- |
| Standard post-reboot result | `/storage/emulated/0/Download/pixel_local__boot-watch-20260607_225750_boot-result.txt` |
| Installed extended run | `/data/adb/boot-watch/runs/20260608_002220_boot` |
| Installed extended archive | `/storage/emulated/0/Download/boot-watch-20260608_002220_boot.tar.gz` |

## Standard post-reboot proof

| Check | Result |
| --- | --- |
| `pbw_result` | `PASS` |
| `pbw_version` | `0.2.5-test.1` |
| `pbw_versionCode` | `25` |
| `pbw_run_id` | `20260607_225750_boot` |
| `pbw_mode` | `boot` |
| profile | `standard` |
| file-name-too-long guard | `absent` |
| protected result names | `yes` |
| Sortify hold expected | `yes` |

Result: **PASS**.

## Installed extended manual proof

| Check | Result |
| --- | --- |
| marker | `RESULT: BOOT_WATCH_BOOT_DONE rc=0` |
| version | `0.2.5-test.1` |
| versionCode | `25` |
| profile | `extended` |
| `PBW_COLLECT_MODULE_LOGS` | `1` |
| module runtime logs enabled | `1` |
| module runtime files | `8` |
| Frosty log files | `3` |
| LSPosed/lspd `log.old` files | `4` |
| file-name-too-long guard | `absent` |
| DB leak check | `absent` |

Result: **PASS**.

## Extended files observed

Frosty bounded tails:

- `module_runtime/frosty/kernel_tweaks.log.txt`
- `module_runtime/frosty/ram.log.txt`
- `module_runtime/frosty/services.log.txt`

LSPosed/lspd rotated bounded tails:

- `module_runtime/lsposed_log_old/kmsg.log.txt`
- `module_runtime/lsposed_log_old/modules_2026-06-07T18:57:24.761472.log.txt`
- `module_runtime/lsposed_log_old/props.txt.txt`
- `module_runtime/lsposed_log_old/verbose_2026-06-07T18:57:24.761143.log.txt`

The archive check found no LSPosed DB files such as `modules_config.db`, `.db-shm`, `.db-wal`, or `.db-journal`.

## Non-blocking runtime observations

The standard post-reboot result also recorded boot-time system pressure that is useful for diagnostics but does not block the Boot Watch release:

- app startup ANRs for WhatsApp and AOK Digen
- high memory pressure and LMKD activity
- transient `settings` / `package` service `Failed transaction` entries
- SELinux denials around app access to `dex2oat` and binderfs transaction logs

These are captured as environment evidence, not as Boot Watch regressions.

## Release decision state

`v0.2.5-test.1` has passed:

1. post-reboot standard profile validation
2. installed extended module-runtime validation
3. DB leak guard validation

It is suitable as the candidate basis for a stable `v0.2.5` decision, while the stable update channel remains on `v0.2.4` until explicitly changed.

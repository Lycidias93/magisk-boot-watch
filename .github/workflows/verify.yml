name: Verify

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify release zip
        run: bash tools/verify-magisk-boot-watch-v0.1.5.1.sh releases/v0.1.5.1/magisk-boot-watch-v0.1.5.1.zip
